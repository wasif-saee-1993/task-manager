import { CanDoDirective } from './can-do.directive';

describe('CanDoDirective', () => {
  it('should create an instance', () => {
    const directive = new CanDoDirective();
    expect(directive).toBeTruthy();
  });
});
